package com.pocketclaw

import org.junit.Test
import org.junit.Assert.assertEquals

/** Placeholder unit test. Real tests added in subsequent phases. */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
